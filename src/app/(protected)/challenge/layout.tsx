export const metadata = {
  title: "Weekly Challenge — DrawLint.ai",
  description:
    "Compete in weekly system design challenges and see how you rank.",
};

export default function ChallengeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
